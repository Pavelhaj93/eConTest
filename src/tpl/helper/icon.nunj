{% macro icon(name, modifier) %}
	<svg class="icon" preserveAspectRatio="none"><use{% if modifier %} class="{{ modifier }}"{% endif %} xlink:href="gfx/icon/svg.svg#{{ name }}" /></svg>
{% endmacro %}
